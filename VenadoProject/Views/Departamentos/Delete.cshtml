@model VenadoProject.Models.ViewModels.departamento

@{
    ViewBag.Title = "Eliminar Registro";
}

<h2>Borrar </h2>

<h3>¿Seguro de borrar?</h3>
<div>
    <h4>departamento</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nombre_departamento)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre_departamento)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Borrar" class="btn btn-default" /> |
            @Html.ActionLink("Regresar", "Index")
        </div>
    }
</div>
